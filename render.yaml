services:
  - type: web
    name: live-meter-api
    env: python  # <--- This is the most important line
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
